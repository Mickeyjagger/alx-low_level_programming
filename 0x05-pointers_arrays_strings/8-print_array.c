#include <stdio.h>
#include "main.h"

/**
 * print_array - prints n digit of an array
 * @a: array
 * @n: number of values
 * Return: a and n inputs
 */
void print_array(int *a, int n)
{
	int inc = 0;

	while (*(src + inc) != '\0')
	{
		*(dest + inc) = *(src + inc);
	}
	*(dest + inc) = '\0';

	return (dest);
}
